package main

import(
	"sort"
)

func longestCommonPrefix(strs []string) string {
	var ascending_list=sort.Strings()
	// maks the current string
	var current_prefix string = ascending_list[len(ascending_list)-1]
	for _, word:=range sts{

		current_prefix=
		for idx, x:= range word{
			if x

			}


		}
	}



	var return_thing string
	return return_thing
}
